lttng_ust_itt_metadata:
  events:
  - name: metadata
    args:
    - [uint64_t, size]
    - [void*, data]
    fields:
    - [ ctf_integer_hex, uintptr_t, data, "(uintptr_t)data" ]
    - [ ctf_sequence_text, char, data_vals, (const char*)data, size_t, "size" ]

